/**
 * What belongs here: subscribes to react-router-dom location changes and
 * calls appInsights.trackPageView. Strips query strings from logged URIs
 * to avoid leaking PII (per api-pii-handling.md / web-error-logging.md).
 *
 * Scaffolded — implementation lands in slice 1.
 */

export const useTrackPageView = (): void => {
  // slice 1
};
