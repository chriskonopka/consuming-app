/**
 * What belongs here: per-collection chat session reducer — current conversation,
 * model picker, streaming buffer, composer text. Auto-resolves the conversation
 * on collection activation; lazy-creates on first send.
 *
 * Scaffolded — implementation lands in slice 3.
 */

import type { ChatSession } from '@shared/types';

export const useChatSession = (): ChatSession | null => {
  return null;
};
