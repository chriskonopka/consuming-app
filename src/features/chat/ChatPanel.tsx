/**
 * What belongs here: slide-in chat panel with composer, message list,
 * status row, model picker, and Clear button. Wires the SSE streaming
 * client (useSseChat) and the conversation lifecycle (useChatSession).
 *
 * Scaffolded — implementation lands in slice 3 (Chat panel + SSE streaming).
 */

export const ChatPanel = () => {
  return (
    <div role="status" aria-label="Chat panel placeholder">
      Chat panel — wired in slice 3.
    </div>
  );
};
